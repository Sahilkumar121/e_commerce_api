ecommerce_api/
├── alembic/                    # Database migrations generated by Alembic
├── app/
│   ├── api/                    # Routing and HTTP request handlers
│   │   ├── dependencies.py     # Reusable injections: get_db, get_current_user, get_admin
│   │   └── v1/
│   │       ├── api.py          # Main router that aggregates all endpoints below
│   │       └── endpoints/
│   │           ├── admin.py    # /admin/dashboard/sales, /admin/orders
│   │           ├── auth.py     # /auth/register, /auth/login
│   │           ├── cart.py     # /cart/items
│   │           ├── orders.py   # /orders/checkout, /orders/me
│   │           ├── products.py # /products, /categories
│   │           └── reviews.py  # /products/{id}/reviews
│   ├── core/                   # App-wide settings and security
│   │   ├── config.py           # Pydantic BaseSettings (loads .env variables)
│   │   ├── exceptions.py       # Custom HTTP exceptions (e.g., InsufficientStockException)
│   │   └── security.py         # JWT creation, password hashing (PassLib)
│   ├── db/                     # Database setup and connections
│   │   ├── database.py         # Async SQLAlchemy engine and session maker
│   │   └── redis.py            # Redis connection pool for caching & idempotency
│   ├── models/                 # SQLAlchemy ORM Models (Database schema)
│   │   ├── base.py             # Declarative base class
│   │   ├── cart.py             # CartItem
│   │   ├── order.py            # Order, OrderItem, Payment
│   │   ├── product.py          # Category, Product, Review
│   │   └── user.py             # User
│   ├── schemas/                # Pydantic Models (Request/Response validation)
│   │   ├── cart.py             # e.g., CartItemCreate, CartItemResponse
│   │   ├── order.py
│   │   ├── product.py
│   │   ├── token.py            # Token payload schemas
│   │   └── user.py
│   ├── services/               # Core Business Logic (The brain of your app)
│   │   ├── checkout.py         # Cart -> Order conversion, inventory DB row locks
│   │   ├── payment.py          # Mock Stripe integration, idempotency key checks
│   │   └── product.py          # Product CRUD, Redis cache invalidation logic
│   ├── tasks/                  # Async Background Tasks
│   │   ├── celery_app.py       # Celery worker configuration
│   │   └── email.py            # Async order confirmation email tasks
│   └── main.py                 # FastAPI application instance, CORS, middleware
├── tests/
│   ├── api/                    # Integration tests for endpoints
│   ├── crud/                   # Unit tests for database operations
│   └── conftest.py             # Pytest fixtures (mock DB, mock Redis)
├── .env                        # Environment variables (DB URL, Secret Keys)
├── alembic.ini                 # Alembic configuration
├── docker-compose.yml          # Easy local setup for PostgreSQL and Redis
└── requirements.txt            # (or pyproject.toml / Pipfile)